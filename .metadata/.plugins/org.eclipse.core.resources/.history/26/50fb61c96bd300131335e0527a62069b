



public class HDFSUploader {
	
	//Will be used to upload the files and generate the sequence file
	sequenceUploader sUploader;
	fileUploader fUploader;
	

	public static void main(String[] args) {
		if (args.length!=1){
			System.out.println("Usage hdfsUploader Configuration/Configuration.json");
		}
		else
		{
			String configurationFile;
			configurationFile=args[0];
			
			configuration conf = new configuration(configurationFile);
			utils ut = new utils();
			
			
			//Move Files to Process
			
			String inputCommonFolder=conf.getInputFolder("Input Common Folder");
			String inputClassifyFolder=conf.getInputFolder("Input Classify Folder");
			String processCommonFolder=conf.getInputFolder("Process Common Folder");
			String processClassifyFolder=conf.getInputFolder("Process Classify Folder");
			
			
			ut.moveFiles(inputCommonFolder, processCommonFolder);
			ut.moveFolder(inputSingleFolder, processSingleFolder);
			
			//sUploader = new sequenceUploader();
			//fUploader = new fileUploader();
		
			//Move Files to Loaded
			
			
			
		
			
			
			
			
			
			
			
		}
		

	}

}
